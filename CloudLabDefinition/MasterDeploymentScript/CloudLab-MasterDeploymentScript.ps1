#
# CloudLab-MasterDeploymentScript.ps1
# This script is the master script which is used to deploy the collection of resources groups that comprise the cloud lab.
# Each separate resource group is named here, but defined within RM templates and parameter files, each with its own Deploy script created by VS.
# This script is used to invoke those other script, enabling a full or piecemeal deployment of the various lab components.
#
